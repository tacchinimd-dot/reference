services:
  - type: web
    name: product-reference-board
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && playwright install-deps chromium && playwright install chromium
    startCommand: python product_board_server.py
    envVars:
      - key: RENDER
        value: "true"
